# 公司新聞蒐集工具 — 開發紀錄

## 專案基本資訊

| 項目 | 內容 |
|------|------|
| GitHub Repo | https://github.com/carolkao-fin/news-crawler |
| Streamlit Cloud | 部署後網址顯示於 share.streamlit.io |
| 主程式（網頁版） | `app.py` |
| 主程式（命令列） | `main.py` |
| 外部 API | 無需 API key（Google News RSS、鉅亨網公開 API） |
| 開發日期 | 2026-08-25 |

---

## 需求來源

訪視報告的第八節「近兩年相關新聞」原本要人工搜尋、複製、逐則排版。目標是輸入公司
名稱後自動完成蒐集與排版，產出與現行檔案（例
`115_A-I-001_54955208_台灣禾邦電子有限公司.doc`）版面一致的 Word 檔。

---

## 功能說明

### 蒐集
1. 由公司全名自動展開關鍵字：`台灣禾邦電子有限公司` → `台灣禾邦電子`／`禾邦電子`／`禾邦`；
   母公司（投資人）與相關企業／轉投資公司可另行填入，各自展開檢索。
2. Google News RSS 全網檢索（限定近 N 年，預設 2 年）。
3. 對第一優先來源網域各做一次 `site:` 定向檢索。
4. 對每一家對象另做兩組關注議題檢索（關稅、地緣政治、科技戰、出口管制、供應鏈、
   AI、減碳…），並替每則新聞標上命中議題。
5. 鉅亨網搜尋 API 補財經新聞。
6. 公司官網「最新消息／新聞中心」列表解析。
7. 去重（網址、標題、同稿異版）、依加權分數排序、逐則抓全文。

### 檢視與產出
- 網頁版可逐則展開、勾選、就地修訂標題與內文，再產出 Word。
- 命令列版可用 `--json` 存下原始資料，人工整理後以 `--from-json` 重新產檔。
- 本機有 Word 時自動轉 `.doc` 並更新目錄頁碼；沒有 Word 則輸出 `.docx`。
- 每次蒐集自動存入歷史紀錄，可載回編輯、重新產檔、下載單筆 JSON 或整批 ZIP。

---

## 來源優先序

依既有的 `新聞來源彙整.xlsx`（30 家公司、194 則新聞的實際統計）決定：

| 排名 | 來源 | 則數 | 涵蓋公司數 |
|---|---|---|---|
| 1 | 經濟日報 | 26 | 11 |
| 2 | 工商時報 | 21 | 9 |
| 3 | MoneyDJ | 11 | 5 |
| 4 | 新浪財經／新浪網 | 9 | 6 |
| 8 | 自由時報 | 4 | 3 |
| 15 | 鉅亨網 | 3 | 1 |
| — | 各公司官網（星宸科技、弘訊科技、宸展光電、DyStar…） | 合計 10+ | 5 |

因此設定為：**第一優先＝經濟日報、工商時報、鉅亨網、公司官網**（額外做定向檢索、
清單標 ★）；第二優先＝MoneyDJ、自由時報、聯合報、中央社、科技新報、DIGITIMES 等
常用媒體；其他媒體照樣蒐集。**來源不設限，優先序是加權不是門檻**——實作方式見下方
技術決策第 7 點。

---

## 檔案結構

```
news_crawler/
├── app.py              # Streamlit 網頁版
├── main.py             # 命令列版
├── crawler.py          # 蒐集核心：關鍵字展開、多來源檢索、網址還原、全文擷取
├── docbuilder.py       # Word 產出：樣式重建、TOC 欄位、頁碼、.doc 轉檔
├── history.py          # 歷史紀錄：存檔、列表、載回、刪除、ZIP 匯出
├── smoke_test.py       # 冒煙測試：實際執行 app.py 並檢查例外
├── history/            # 歷史紀錄資料夾（執行時自動產生，不上傳）
├── requirements.txt
├── .gitignore
├── README.md
└── 開發紀錄.md          # 本文件
```

---

## 套件清單（requirements.txt）

```
streamlit>=1.33
requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
python-docx>=1.1
```

無需 API key，也不需要瀏覽器驅動。

---

## 技術決策

### 1. 格式規格用讀的，不用目測

沒有憑範例文字的外觀去模仿排版，而是先用 Word COM 把原始 `.doc` 的欄位與樣式
規格 dump 出來，才發現兩件事：

- 新聞目錄不是手打的，是 Word 欄位 `TOC \h \z \t "title2.0,1"`，頁碼由 Word 產生。
- 內文用的是五個自訂樣式，規格如下（`docbuilder.py` 以相同名稱重建）：

| 樣式 | 字型 | 字級 | 其他 |
|---|---|---|---|
| `廠商 - 目錄 標 1` | 標楷體 | 18pt 粗體 | 最小行高 20pt |
| `title2.0` | Microsoft YaHei | 16pt 粗體 | 段前 12pt；目錄由此樣式產生 |
| `廠商 - 新聞來源` | 標楷體 | 11pt | 最小行高 12pt |
| `廠商 - 新聞 2` | 標楷體 | 13pt 粗體 | 單行行距 |
| `廠商 - 新聞內容` | 標楷體 | 13pt | 左右對齊、**固定行高 24pt**、首行縮排 2 字元 |

兩個踩點記錄下來：

- Word 的 `LineSpacingRule` 是代碼不是字面值：`0`＝單行、`3`＝最小行高、`4`＝固定行高。
  只抄 `LineSpacing=24` 而沒抄 rule，行距會完全不一樣。
- 「首行縮排 2 **字元**」在 OOXML 是 `w:ind/@w:firstLineChars="200"`，python-docx
  沒有對應 API，要自己補 XML；若改用 `first_line_indent=Pt(26)` 換成固定點數，
  字級一改就跑掉。

### 2. 不用範本檔，改程式化重建樣式

原本想把原始 `.doc` 轉成空白 `template.docx` 當範本，但 Streamlit Cloud 是 Linux，
沒有 Word，且範本檔會讓 repo 綁死在某一份客戶文件上。改成在 `docbuilder.py` 裡以
相同樣式名稱與規格程式化建立，跨平台可跑，也不必把客戶檔案推上 GitHub。

### 3. Google News 網址還原

Google News RSS 給的是 `news.google.com/rss/articles/CBMi...` 轉址，直接 GET 會拿到
一個空殼頁（`data-n-au` 之類的舊寫法早就失效）。實際可行的做法是讀該頁的
`data-n-a-id` / `data-n-a-ts` / `data-n-a-sg` 三個簽章屬性，再 POST 到
`news.google.com/_/DotsSplashUi/data/batchexecute` 的 `Fbv4je`（`garturlreq`）取回
真實網址。這是 Google 內部介面，改版就可能失效，因此失敗時保留原轉址連結並在結果
標示錯誤，不讓整批中斷。

### 4. 全文擷取的三層策略與雜訊處理

擷取順序：站台專屬 CSS 選擇器 → JSON-LD `articleBody` → 段落密度最高的區塊。

雜訊分兩種處理方式，這個區分很關鍵：

- **只丟這一段**（`_DROP_PAT`）：廣告、字級設定、「已將網址複製到剪貼簿」等。
- **從這裡截斷**（`_END_PAT`）：「更多○○報導」「延伸閱讀」「◎本資料僅供參考」等。
  這類字樣代表正文已結束，後面全是推薦新聞。

一開始兩者混用，結果 Yahoo 股市一篇 452 字的新聞被抓成 3,866 字，多出來的全是文末
夾帶的數十則推薦新聞標題。改成截斷後才正確。文末另外再砍掉連續的短句（無句號、
34 字以內），處理工商時報頁尾的相關新聞連結列。

### 5. 官網抓取與備援

官網結構各家不同，做法是：從首頁找出文字或網址含「新聞／消息／公告／news／press」
的連結當列表頁 → 解析列表頁的連結文字與鄰近文字取日期 → 逐則抓全文。實測華新麗華
官網可正確取得 11 則帶日期的消息。

但購物站型的官網（實測維格餅家）是 JS 動態渲染，靜態 HTML 只有 `{{word('read_more')}}`
這種樣板字串，抓不到任何項目。因此加了備援：官網靜態解析結果為 0 時，自動改用
`site:官網網域` 的 Google 定向檢索。

### 6. 時區

Google News 給 UTC、鉅亨網給 epoch、官網解析出來的是無時區的日期，混在一起排序會
丟出 `can't compare offset-naive and offset-aware datetimes`。統一在建立 Article 時
就轉成「台北時間、無時區」的 naive datetime，不留到排序才處理。

### 7. 優先序用加權，不用硬性分層

第一版把來源分成三層、以層級當排序主鍵，結果是第一優先來源會先把名額填滿，其他媒體
即使有更新更相關的新聞也整批進不來——這是「限定來源」不是「優先來源」。改成加權：
以「幾天前」當基準分數，第一優先來源扣 180 天、第二優先扣 60 天，標題沒命中關鍵字
加罰 365 天，分數小的排前面。

效果是「同期新聞優先來源勝出，但夠新的其他媒體照樣排進來」：實測維格餅家一案，8 篇
中 7 篇來自工商時報與鉅亨網，另一篇香港 01 的報導仍排在第 5 位。加權天數可調，設 0
就退回純日期排序。

### 8. 同稿異版的去重

同一則新聞常同時出現在媒體與公司官網，標題只差幾個字（實測華新麗華一則產學合作新聞，
工商時報版結尾多了「領域」兩字），比對前 40 字會判成兩則。改成先比正規化全名，再做
一次前綴包含檢查（較短者長度 ≥ 12 且為較長者的前綴即視為重複），才正確合併。

### 9. 編碼判斷不能靠 requests 預設

有幾則新聞下載後整篇是亂碼。原因是 `requests` 遇到沒有標明 charset 的 `text/html`
會依 HTTP 規範預設 ISO-8859-1，Big5／GB 網頁因此逐字被拆成拉丁字元；原本用
`resp.apparent_encoding` 補救，但那是統計猜測，短文與混排頁常猜錯，而且會忽略
HTML 裡明寫的 `<meta charset>`。

改成 `decode_html()`：依「HTTP 標頭 charset → HTML meta charset → 候選清單逐一試解」
的順序，每種解法算一次**亂碼比例**（U+FFFD 加上 U+0080~U+00FF 的佔比），低於 0.5%
就直接採用，否則取比例最低者。另外把 `big5` 一律對應到 `cp950`、`gb2312`／`gbk`
對應到 `gb18030`，避免罕用字缺字；標頭明寫 `iso-8859-1` 時直接忽略該值。實測
Big5 有 meta、UTF-8 有 meta、GB 無 meta、標頭謊報 iso-8859-1 四種情況都正確還原。

### 10. 依關注議題與關聯企業檢索

訪視報告關心的不是公司的所有新聞，而是母公司（投資人）、受訪公司與其相關企業／
轉投資公司在特定議題上的動向。因此加了兩層：

**檢索對象**：`related_companies` 可填母公司與相關企業，各自展開關鍵字檢索，
結果標明 `entity`（是為哪一家找到的）。相關性判定改用「所有對象的關鍵字聯集」。

**關注議題**：內建 14 類議題詞庫（關稅、地緣政治、科技戰、國安、資安、出口管制、
電動車／車用、供應鏈、國產化、AI、機器人、量子科技、能源減碳、數位化），對每一家
對象另下兩組議題檢索，並替每則新聞標上命中議題；命中者排序時每項扣 120 天，最多
計三項。

兩個實作細節：

- **英數關鍵字要加詞界**。`AI` 直接做子字串比對會命中 `said`、`chain`、`detail`、
  `retail`，整批英文新聞都會被誤標。改成 ASCII 關鍵字用
  `(?<![A-Za-z0-9])ai(?![A-Za-z0-9])`，中文維持單純比對。
- **供應鏈轉移要用複合條件**。單靠地區詞會失控——「美國」幾乎每則財經新聞都出現。
  改成地區詞（東南亞／北美／歐洲／印度）與移轉動作詞（設廠、擴產、遷廠、產能、
  布局…）同時出現才算，並標出地區。

實測台灣禾邦電子＋華新科技一案，10 篇全部帶議題標籤（AI、能源減碳、關稅、出口管制、
供應鏈轉移），其中 4 篇來自關聯企業，內容明顯比純公司名檢索貼近報告需求。

### 11. Streamlit Cloud 的模組快取

推上新版後雲端出現 `TypeError`，堆疊只停在 `app.py` 呼叫 `collect_news(...)` 那一行、
沒有進到 `crawler.py`——這代表錯在**參數綁定**，也就是雲端跑的 `crawler.py` 是舊版。
原因是 Streamlit Cloud 在 repo 更新時會重跑主程式，但已 `import` 的模組留在
`sys.modules` 不會重載，於是 `app.py` 是新版、`crawler.py` 是舊版。

處理方式：三個檔案各帶 `__version__`，`app.py` 啟動時比對，不一致就
`importlib.reload()`；重載後仍不一致才顯示訊息請使用者 Reboot app。順帶記一個判讀
技巧：**堆疊只到呼叫端那一行、沒有進入被呼叫函式，就是參數綁定失敗**，該往版本或
簽章不一致的方向查，不必去找函式內部的邏輯錯誤。

### 12. 議題也是加權——同一個坑踩第二次

把來源分層改成加權之後（技術決策 7），議題這一層又重演了同樣的問題：對每一家對象
額外下兩組議題檢索，候選池因此被命中議題的新聞灌滿，再疊上「每項議題扣 120 天、
最多三項」的加權，一般新聞（法說會、營收、人事、市場行情）幾乎排不進名額。使用者
說的是「盡量以這些議題為主」，實作出來卻接近「只有這些議題」。

兩處修正：

- 議題加權上限由三項降為兩項（最多 240 天），避免單篇標到五個議題就壓過所有新聞。
- 加入 `_reserve_slots()`：保留 25% 名額給未命中議題的新聞。

`_reserve_slots()` 第一版寫成「議題新聞放前面、非議題新聞插在後面」，反而製造新
問題——原本排第一的非議題新聞會被推到第五名之後。改成照原排序逐一取用，只在議題
新聞取滿 `max_articles - reserve` 篇之後才把後續議題新聞擱置讓位，被擱置的接在
後面不丟棄。三種情境都做了單元驗證：最高分是非議題新聞時不被推後、清一色議題新聞
時非議題會被拉進保留名額、候選中完全沒有非議題新聞時原樣返回。

順序上還有一個細節：保留名額必須在候選池截斷**之前**套用，否則截斷會先把非議題
新聞砍掉，保留就沒有對象了。

### 13. 歷史紀錄的儲存設計（功能已於 2026-08-27 移除，見第 15 節）

每次蒐集自動存一筆 JSON，內容含案件欄位、使用的關鍵字與參數、來源分布與全部新聞
全文，之後可以載回編輯、重新產檔、下載或打包成 ZIP。

三個設計選擇：

- **一筆一檔，不做索引檔**。索引由掃描資料夾即時產生。若維護一份 `index.json`，
  命令列與網頁版同時執行時會互相覆蓋；一筆一檔則天然無衝突，壞掉一筆也不影響其他。
- **`Article.from_dict()` 與 `to_dict()` 對稱**。歷史紀錄、`--json` 匯出、網頁版
  下載共用同一種格式，載回後就是完整的 Article 物件，可以直接丟給 `build_docx()`。
- **儲存位置可用環境變數覆寫**（`NEWS_CRAWLER_HISTORY`），預設在程式資料夾下的
  `history/`，並加進 `.gitignore`。

一個必須講清楚的限制：Streamlit Cloud 的檔案系統是暫存的，重新部署或重啟就清空，
而且同一個 App 的所有使用者共用同一份歷史紀錄。這在 UI 與 README 都明白標示，並
提供 ZIP 下載作為長期保存手段；敏感案件應該用本機版執行。這不是可以靠寫程式繞過
的問題，只能讓使用者知道。

### 14. 兩個自己造成的錯誤，與對應的防呆

加入歷史紀錄那次，雲端出現 `NameError: name '_history' is not defined`。兩個原因疊
在一起：

**其一，`str.replace()` 靜默失敗。** 我用一段含 `APP_VERSION = "1.2.0"` 的舊字串去
比對，但檔案當時已經是 `1.3.0`，替換不成立卻不會報錯，於是 `import history as
_history` 沒被加進去；而另一段加入 `_history` 重載的替換不含版本號，照樣成功。結果
是變數被引用卻從未匯入。**教訓：每個 `replace` 都要 assert 舊字串存在**，這在其他
段落有做，唯獨這裡漏了。

**其二，冒煙測試測錯東西。** 我原本用 `streamlit run` 加 `curl` 檢查 HTTP 200，
但 Streamlit 的健康檢查在主程式執行前就回 200——腳本要等瀏覽器透過 websocket 連上
才會跑。所以啟動 log 乾淨、HTTP 200，程式其實一執行就炸。改用
`streamlit.testing.v1.AppTest` 真的把腳本執行一遍，並寫成 `smoke_test.py`：檢查三個
模組版本一致、`app.py` 的 `APP_VERSION` 同步、執行無例外、頁面元件有正常產生。

寫完後我把 import 那行拿掉重跑一次，確認測試會失敗並印出同一個 `NameError`——
**測不出已知錯誤的測試沒有價值**，這一步不能省。

### 15. 移除歷史紀錄功能（2026-08-27）

第 13 節列出的限制最後成為移除這個功能的理由。歷史紀錄寫在容器本機檔案系統，而
Streamlit Community Cloud 的容器沒有掛載持久化磁碟，重新部署、重啟或休眠喚醒都會
重建容器；`history/` 又在 `.gitignore` 裡，新容器啟動時就是空的。更關鍵的是隔離
問題：Streamlit 的隔離單位是 session 不是 process，同一個 App 的所有使用者共用同一
份檔案系統，而 `list_runs()` 是直接掃資料夾、不分使用者，所以任何人都看得到別人查
過的公司、案號與統編。

要在雲端同時解決這兩件事，需要外部持久化儲存加上使用者認證與資料分權，兩者都遠超
這個工具的定位。與其留著一個「會消失、又人人可見」的功能並靠警語提醒，不如拿掉：
`--json` / `--from-json` 與網頁版的下載按鈕已經涵蓋「把這次結果留下來、之後重跑」
的需求，而且產物在使用者自己手上，語意上不會讓人誤以為系統有在保管資料。

移除範圍：刪除 `history.py` 與 `history/`；`app.py` 拿掉 import、版本檢查項、蒐集
後自動存檔，以及頁面最下方整個「📚 歷史紀錄」區塊；`main.py` 拿掉 `--list-history`
／`--from-history`／`--no-history` 三個參數與對應處理；`smoke_test.py` 的模組版本
檢查由三個模組改為兩個；`.gitignore` 與 README 對應段落一併移除。三個模組版本號同
步升到 `1.5.0`，讓 Streamlit Cloud 依第 11 節的機制強制重載，避免雲端留著舊的
`history` 模組。

`Article.from_dict()` 保留——它與 `to_dict()` 對稱，`--json` 匯出與網頁版下載仍在用。

---

## 開發歷程

| 階段 | 內容 |
|---|---|
| 1 | 讀取原始 `.doc`：以 Word COM dump 出 27 個欄位與五個自訂樣式的完整規格 |
| 2 | 驗證來源可行性：Google News RSS、Google 網址還原、鉅亨網 API 逐一實測 |
| 3 | 寫 `crawler.py`（蒐集）與 `docbuilder.py`（Word 產出） |
| 4 | 端到端測試（維格餅家、台灣禾邦電子），以 Word COM 比對產出的樣式與目錄 |
| 5 | 修正擷取雜訊：工商時報剪貼簿提示、Yahoo 文末推薦新聞 |
| 6 | 依 `新聞來源彙整.xlsx` 加入來源分級、定向檢索、來源名稱正規化 |
| 7 | 加入官網抓取與 JS 網站備援；修正時區混用問題 |
| 8 | 網頁版 `app.py`、README、本開發紀錄，推上 GitHub |
| 9 | 依回饋把來源分層改為加權排序（不限來源、只做優先），並修正同稿異版去重 |
| 10 | 修正 Big5／GB 網頁亂碼；加入關注議題詞庫與母公司／相關企業檢索 |
| 11 | 加入模組版本檢查與自動重載，修正 Streamlit Cloud 更新後的 TypeError |
| 12 | 議題改為加權並保留其他題材名額（不限於指定議題） |
| 13 | 加入歷史紀錄：自動存檔、載回編輯、重新產檔、單筆與整批下載 |
| 14 | 修正漏掉的 import；改用 AppTest 寫真正會執行主程式的冒煙測試 |
| 15 | 移除歷史紀錄功能（雲端不持久、且同 App 使用者共用，無法只靠程式解決） |

---

## 驗證方式

產出的 `.doc` 用 Word COM 逐段落 dump 樣式名稱，與原始檔案比對：

- 段落樣式序列一致：`廠商 - 目錄 標 1` → `內文`（新聞目錄）→ `目錄 1`（TOC 自動產生，
  含頁碼）→ `title2.0` → `廠商 - 新聞來源` → `廠商 - 新聞內容`。
- 內文樣式規格一致：13pt、標楷體、`LineSpacingRule=4`、`LineSpacing=24`、
  `CharacterUnitFirstLineIndent=2`、`Alignment=3`（左右對齊）。
- 目錄頁碼正確生成（測試檔 15 頁，目錄頁碼 1／2／7／12／13）。

---

## 已知限制

1. **付費牆**：經濟日報、工商時報部分內容需訂閱，抓到的可能只有前段。結果會顯示
   字數，網頁版可直接補貼內文再產檔。
2. **Google 網址還原**依賴內部介面，改版可能失效；屆時全文擷取會失敗但不會中斷，
   結果會標示錯誤訊息。
3. **JS 動態官網**靜態抓不到，會退回 `site:` 定向檢索；若要完整抓取需要瀏覽器
   自動化（playwright），目前刻意不加，以免 Streamlit Cloud 部署變複雜。
4. **關鍵字命中不等於相關**：同名公司、只是順帶提及的新聞都可能入列（實測「維格餅家」
   撈到一則講香港中英街的新聞）。這是網頁版做逐則勾選確認的原因。
5. **議題標籤是關鍵詞比對，不是語意判讀**：文中順帶提到的詞也會被標上，標籤用於
   排序與人工篩選，不應直接當成分析結論。
6. **公開資訊觀測站**目前只列在來源正規化字典裡，尚未做重大訊息的直接查詢；要做需要
   公司代號而非統編。
7. 請遵守各新聞網站使用條款；程式內建請求節流（預設 0.8 秒），勿調得過短。
