# 公司新聞蒐集工具 — 開發紀錄

## 專案基本資訊

| 項目 | 內容 |
|------|------|
| GitHub Repo | https://github.com/carolkao-fin/news-crawler |
| Streamlit Cloud | 部署後網址顯示於 share.streamlit.io |
| 主程式（網頁版） | `app.py` |
| 主程式（命令列） | `main.py` |
| 外部 API | 無需 API key（Google News RSS、鉅亨網公開 API） |
| 開發日期 | 2026-08-25 |

---

## 需求來源

訪視報告的第八節「近兩年相關新聞」原本要人工搜尋、複製、逐則排版。目標是輸入公司
名稱後自動完成蒐集與排版，產出與現行檔案（例
`115_A-I-001_54955208_台灣禾邦電子有限公司.doc`）版面一致的 Word 檔。

---

## 功能說明

### 蒐集
1. 由公司全名自動展開關鍵字：`台灣禾邦電子有限公司` → `台灣禾邦電子`／`禾邦電子`／`禾邦`；
   母公司（投資人）與相關企業／轉投資公司可另行填入，各自展開檢索。
2. Google News RSS 全網檢索（限定近 N 年，預設 2 年）。
3. 對第一優先來源網域各做一次 `site:` 定向檢索。
4. 對每一家對象另做兩組關注議題檢索（關稅、地緣政治、科技戰、出口管制、供應鏈、
   AI、減碳…），並替每則新聞標上命中議題。
5. 鉅亨網搜尋 API 補財經新聞。
6. 公司官網「最新消息／新聞中心」列表解析。
7. 去重（網址、標題、同稿異版）、依加權分數排序、逐則抓全文。

### 檢視與產出
- 網頁版可逐則展開、勾選、就地修訂標題與內文，再產出 Word；「重新蒐集」清空結果
  與修訂（同一家改條件重查），「清除輸入」連案件欄位一起清（換下一家）。
- 命令列版可用 `--json` 存下原始資料，人工整理後以 `--from-json` 重新產檔。
- 本機有 Word 時自動轉 `.doc` 並更新目錄頁碼；沒有 Word 則輸出 `.docx`。

---

## 來源優先序

依既有的 `新聞來源彙整.xlsx`（30 家公司、194 則新聞的實際統計）決定：

| 排名 | 來源 | 則數 | 涵蓋公司數 |
|---|---|---|---|
| 1 | 經濟日報 | 26 | 11 |
| 2 | 工商時報 | 21 | 9 |
| 3 | MoneyDJ | 11 | 5 |
| 4 | 新浪財經／新浪網 | 9 | 6 |
| 8 | 自由時報 | 4 | 3 |
| 15 | 鉅亨網 | 3 | 1 |
| — | 各公司官網（星宸科技、弘訊科技、宸展光電、DyStar…） | 合計 10+ | 5 |

因此設定為：**第一優先＝經濟日報、工商時報、鉅亨網、公司官網**（額外做定向檢索、
清單標 ★）；第二優先＝MoneyDJ、自由時報、聯合報、中央社、科技新報、DIGITIMES 等
常用媒體；其他媒體照樣蒐集。**來源不設限，優先序是加權不是門檻**——實作方式見下方
技術決策第 7 點。

---

## 檔案結構

```
news_crawler/
├── app.py              # Streamlit 網頁版
├── main.py             # 命令列版
├── crawler.py          # 蒐集核心：關鍵字展開、多來源檢索、網址還原、全文擷取
├── docbuilder.py       # Word 產出：樣式重建、TOC 欄位、頁碼、.doc 轉檔
├── smoke_test.py       # 冒煙測試：實際執行 app.py 並檢查例外
├── requirements.txt
├── .gitignore
├── README.md
└── 開發紀錄.md          # 本文件
```

---

## 套件清單（requirements.txt）

```
streamlit>=1.33
requests>=2.31
feedparser>=6.0
beautifulsoup4>=4.12
lxml>=5.0
python-docx>=1.1
```

無需 API key，也不需要瀏覽器驅動。

---

## 技術決策

### 1. 格式規格用讀的，不用目測

沒有憑範例文字的外觀去模仿排版，而是先用 Word COM 把原始 `.doc` 的欄位與樣式
規格 dump 出來，才發現兩件事：

- 新聞目錄不是手打的，是 Word 欄位 `TOC \h \z \t "title2.0,1"`，頁碼由 Word 產生。
- 內文用的是五個自訂樣式，規格如下（`docbuilder.py` 以相同名稱重建）：

| 樣式 | 字型 | 字級 | 其他 |
|---|---|---|---|
| `廠商 - 目錄 標 1` | 標楷體 | 18pt 粗體 | 最小行高 20pt |
| `title2.0` | Microsoft YaHei | 16pt 粗體 | 段前 12pt；目錄由此樣式產生 |
| `廠商 - 新聞來源` | 標楷體 | 11pt | 最小行高 12pt |
| `廠商 - 新聞 2` | 標楷體 | 13pt 粗體 | 單行行距 |
| `廠商 - 新聞內容` | 標楷體 | 13pt | 左右對齊、**固定行高 24pt**、首行縮排 2 字元 |

兩個踩點記錄下來：

- Word 的 `LineSpacingRule` 是代碼不是字面值：`0`＝單行、`3`＝最小行高、`4`＝固定行高。
  只抄 `LineSpacing=24` 而沒抄 rule，行距會完全不一樣。
- 「首行縮排 2 **字元**」在 OOXML 是 `w:ind/@w:firstLineChars="200"`，python-docx
  沒有對應 API，要自己補 XML；若改用 `first_line_indent=Pt(26)` 換成固定點數，
  字級一改就跑掉。

### 2. 不用範本檔，改程式化重建樣式

原本想把原始 `.doc` 轉成空白 `template.docx` 當範本，但 Streamlit Cloud 是 Linux，
沒有 Word，且範本檔會讓 repo 綁死在某一份客戶文件上。改成在 `docbuilder.py` 裡以
相同樣式名稱與規格程式化建立，跨平台可跑，也不必把客戶檔案推上 GitHub。

### 3. Google News 網址還原

Google News RSS 給的是 `news.google.com/rss/articles/CBMi...` 轉址，直接 GET 會拿到
一個空殼頁（`data-n-au` 之類的舊寫法早就失效）。實際可行的做法是讀該頁的
`data-n-a-id` / `data-n-a-ts` / `data-n-a-sg` 三個簽章屬性，再 POST 到
`news.google.com/_/DotsSplashUi/data/batchexecute` 的 `Fbv4je`（`garturlreq`）取回
真實網址。這是 Google 內部介面，改版就可能失效，因此失敗時保留原轉址連結並在結果
標示錯誤，不讓整批中斷。

### 4. 全文擷取的三層策略與雜訊處理

擷取順序：站台專屬 CSS 選擇器 → JSON-LD `articleBody` → 段落密度最高的區塊。

雜訊分兩種處理方式，這個區分很關鍵：

- **只丟這一段**（`_DROP_PAT`）：廣告、字級設定、「已將網址複製到剪貼簿」等。
- **從這裡截斷**（`_END_PAT`）：「更多○○報導」「延伸閱讀」「◎本資料僅供參考」等。
  這類字樣代表正文已結束，後面全是推薦新聞。

一開始兩者混用，結果 Yahoo 股市一篇 452 字的新聞被抓成 3,866 字，多出來的全是文末
夾帶的數十則推薦新聞標題。改成截斷後才正確。文末另外再砍掉連續的短句（無句號、
34 字以內），處理工商時報頁尾的相關新聞連結列。

### 5. 官網抓取與備援

官網結構各家不同，做法是：從首頁找出文字或網址含「新聞／消息／公告／news／press」
的連結當列表頁 → 解析列表頁的連結文字與鄰近文字取日期 → 逐則抓全文。實測華新麗華
官網可正確取得 11 則帶日期的消息。

但購物站型的官網（實測維格餅家）是 JS 動態渲染，靜態 HTML 只有 `{{word('read_more')}}`
這種樣板字串，抓不到任何項目。因此加了備援：官網靜態解析結果為 0 時，自動改用
`site:官網網域` 的 Google 定向檢索。

### 6. 時區

Google News 給 UTC、鉅亨網給 epoch、官網解析出來的是無時區的日期，混在一起排序會
丟出 `can't compare offset-naive and offset-aware datetimes`。統一在建立 Article 時
就轉成「台北時間、無時區」的 naive datetime，不留到排序才處理。

### 7. 優先序用加權，不用硬性分層

第一版把來源分成三層、以層級當排序主鍵，結果是第一優先來源會先把名額填滿，其他媒體
即使有更新更相關的新聞也整批進不來——這是「限定來源」不是「優先來源」。改成加權：
以「幾天前」當基準分數，第一優先來源扣 180 天、第二優先扣 60 天，標題沒命中關鍵字
加罰 365 天，分數小的排前面。

效果是「同期新聞優先來源勝出，但夠新的其他媒體照樣排進來」：實測維格餅家一案，8 篇
中 7 篇來自工商時報與鉅亨網，另一篇香港 01 的報導仍排在第 5 位。加權天數可調，設 0
就退回純日期排序。

### 8. 同稿異版的去重

同一則新聞常同時出現在媒體與公司官網，標題只差幾個字（實測華新麗華一則產學合作新聞，
工商時報版結尾多了「領域」兩字），比對前 40 字會判成兩則。改成先比正規化全名，再做
一次前綴包含檢查（較短者長度 ≥ 12 且為較長者的前綴即視為重複），才正確合併。

### 9. 編碼判斷不能靠 requests 預設

有幾則新聞下載後整篇是亂碼。原因是 `requests` 遇到沒有標明 charset 的 `text/html`
會依 HTTP 規範預設 ISO-8859-1，Big5／GB 網頁因此逐字被拆成拉丁字元；原本用
`resp.apparent_encoding` 補救，但那是統計猜測，短文與混排頁常猜錯，而且會忽略
HTML 裡明寫的 `<meta charset>`。

改成 `decode_html()`：依「HTTP 標頭 charset → HTML meta charset → 候選清單逐一試解」
的順序，每種解法算一次**亂碼比例**（U+FFFD 加上 U+0080~U+00FF 的佔比），低於 0.5%
就直接採用，否則取比例最低者。另外把 `big5` 一律對應到 `cp950`、`gb2312`／`gbk`
對應到 `gb18030`，避免罕用字缺字；標頭明寫 `iso-8859-1` 時直接忽略該值。實測
Big5 有 meta、UTF-8 有 meta、GB 無 meta、標頭謊報 iso-8859-1 四種情況都正確還原。

### 10. 依關注議題與關聯企業檢索

訪視報告關心的不是公司的所有新聞，而是母公司（投資人）、受訪公司與其相關企業／
轉投資公司在特定議題上的動向。因此加了兩層：

**檢索對象**：`related_companies` 可填母公司與相關企業，各自展開關鍵字檢索，
結果標明 `entity`（是為哪一家找到的）。相關性判定改用「所有對象的關鍵字聯集」。

**關注議題**：內建 14 類議題詞庫（關稅、地緣政治、科技戰、國安、資安、出口管制、
電動車／車用、供應鏈、國產化、AI、機器人、量子科技、能源減碳、數位化），對每一家
對象另下兩組議題檢索，並替每則新聞標上命中議題；命中者排序時每項扣 120 天，最多
計三項。

兩個實作細節：

- **英數關鍵字要加詞界**。`AI` 直接做子字串比對會命中 `said`、`chain`、`detail`、
  `retail`，整批英文新聞都會被誤標。改成 ASCII 關鍵字用
  `(?<![A-Za-z0-9])ai(?![A-Za-z0-9])`，中文維持單純比對。
- **供應鏈轉移要用複合條件**。單靠地區詞會失控——「美國」幾乎每則財經新聞都出現。
  改成地區詞（東南亞／北美／歐洲／印度）與移轉動作詞（設廠、擴產、遷廠、產能、
  布局…）同時出現才算，並標出地區。

實測台灣禾邦電子＋華新科技一案，10 篇全部帶議題標籤（AI、能源減碳、關稅、出口管制、
供應鏈轉移），其中 4 篇來自關聯企業，內容明顯比純公司名檢索貼近報告需求。

### 11. Streamlit Cloud 的模組快取

推上新版後雲端出現 `TypeError`，堆疊只停在 `app.py` 呼叫 `collect_news(...)` 那一行、
沒有進到 `crawler.py`——這代表錯在**參數綁定**，也就是雲端跑的 `crawler.py` 是舊版。
原因是 Streamlit Cloud 在 repo 更新時會重跑主程式，但已 `import` 的模組留在
`sys.modules` 不會重載，於是 `app.py` 是新版、`crawler.py` 是舊版。

處理方式：三個檔案各帶 `__version__`，`app.py` 啟動時比對，不一致就
`importlib.reload()`；重載後仍不一致才顯示訊息請使用者 Reboot app。順帶記一個判讀
技巧：**堆疊只到呼叫端那一行、沒有進入被呼叫函式，就是參數綁定失敗**，該往版本或
簽章不一致的方向查，不必去找函式內部的邏輯錯誤。

### 12. 議題也是加權——同一個坑踩第二次

把來源分層改成加權之後（技術決策 7），議題這一層又重演了同樣的問題：對每一家對象
額外下兩組議題檢索，候選池因此被命中議題的新聞灌滿，再疊上「每項議題扣 120 天、
最多三項」的加權，一般新聞（法說會、營收、人事、市場行情）幾乎排不進名額。使用者
說的是「盡量以這些議題為主」，實作出來卻接近「只有這些議題」。

兩處修正：

- 議題加權上限由三項降為兩項（最多 240 天），避免單篇標到五個議題就壓過所有新聞。
- 加入 `_reserve_slots()`：保留 25% 名額給未命中議題的新聞。

`_reserve_slots()` 第一版寫成「議題新聞放前面、非議題新聞插在後面」，反而製造新
問題——原本排第一的非議題新聞會被推到第五名之後。改成照原排序逐一取用，只在議題
新聞取滿 `max_articles - reserve` 篇之後才把後續議題新聞擱置讓位，被擱置的接在
後面不丟棄。三種情境都做了單元驗證：最高分是非議題新聞時不被推後、清一色議題新聞
時非議題會被拉進保留名額、候選中完全沒有非議題新聞時原樣返回。

順序上還有一個細節：保留名額必須在候選池截斷**之前**套用，否則截斷會先把非議題
新聞砍掉，保留就沒有對象了。

### 13. 歷史紀錄的儲存設計（功能已於 2026-08-27 移除，見第 15 節）

每次蒐集自動存一筆 JSON，內容含案件欄位、使用的關鍵字與參數、來源分布與全部新聞
全文，之後可以載回編輯、重新產檔、下載或打包成 ZIP。

三個設計選擇：

- **一筆一檔，不做索引檔**。索引由掃描資料夾即時產生。若維護一份 `index.json`，
  命令列與網頁版同時執行時會互相覆蓋；一筆一檔則天然無衝突，壞掉一筆也不影響其他。
- **`Article.from_dict()` 與 `to_dict()` 對稱**。歷史紀錄、`--json` 匯出、網頁版
  下載共用同一種格式，載回後就是完整的 Article 物件，可以直接丟給 `build_docx()`。
- **儲存位置可用環境變數覆寫**（`NEWS_CRAWLER_HISTORY`），預設在程式資料夾下的
  `history/`，並加進 `.gitignore`。

一個必須講清楚的限制：Streamlit Cloud 的檔案系統是暫存的，重新部署或重啟就清空，
而且同一個 App 的所有使用者共用同一份歷史紀錄。這在 UI 與 README 都明白標示，並
提供 ZIP 下載作為長期保存手段；敏感案件應該用本機版執行。這不是可以靠寫程式繞過
的問題，只能讓使用者知道。

### 14. 兩個自己造成的錯誤，與對應的防呆

加入歷史紀錄那次，雲端出現 `NameError: name '_history' is not defined`。兩個原因疊
在一起：

**其一，`str.replace()` 靜默失敗。** 我用一段含 `APP_VERSION = "1.2.0"` 的舊字串去
比對，但檔案當時已經是 `1.3.0`，替換不成立卻不會報錯，於是 `import history as
_history` 沒被加進去；而另一段加入 `_history` 重載的替換不含版本號，照樣成功。結果
是變數被引用卻從未匯入。**教訓：每個 `replace` 都要 assert 舊字串存在**，這在其他
段落有做，唯獨這裡漏了。

**其二，冒煙測試測錯東西。** 我原本用 `streamlit run` 加 `curl` 檢查 HTTP 200，
但 Streamlit 的健康檢查在主程式執行前就回 200——腳本要等瀏覽器透過 websocket 連上
才會跑。所以啟動 log 乾淨、HTTP 200，程式其實一執行就炸。改用
`streamlit.testing.v1.AppTest` 真的把腳本執行一遍，並寫成 `smoke_test.py`：檢查三個
模組版本一致、`app.py` 的 `APP_VERSION` 同步、執行無例外、頁面元件有正常產生。

寫完後我把 import 那行拿掉重跑一次，確認測試會失敗並印出同一個 `NameError`——
**測不出已知錯誤的測試沒有價值**，這一步不能省。

### 15. 移除歷史紀錄功能（2026-08-27）

第 13 節列出的限制最後成為移除這個功能的理由。歷史紀錄寫在容器本機檔案系統，而
Streamlit Community Cloud 的容器沒有掛載持久化磁碟，重新部署、重啟或休眠喚醒都會
重建容器；`history/` 又在 `.gitignore` 裡，新容器啟動時就是空的。更關鍵的是隔離
問題：Streamlit 的隔離單位是 session 不是 process，同一個 App 的所有使用者共用同一
份檔案系統，而 `list_runs()` 是直接掃資料夾、不分使用者，所以任何人都看得到別人查
過的公司、案號與統編。

要在雲端同時解決這兩件事，需要外部持久化儲存加上使用者認證與資料分權，兩者都遠超
這個工具的定位。與其留著一個「會消失、又人人可見」的功能並靠警語提醒，不如拿掉：
命令列版的 `--json` / `--from-json` 已經涵蓋「把這次結果留下來、之後重跑」的需求，
而且產物在使用者自己手上，語意上不會讓人誤以為系統有在保管資料。

網頁版隨後也拿掉了「下載原始資料（JSON）」按鈕（`import json` 一併移除）。網頁版
的定位是「查完、挑完、產出 Word」的一次性流程，中繼的原始資料對使用者沒有意義，
多一顆按鈕只是多一個要解釋的東西；真的需要保留原始資料再跑一次的情境，用命令列版
的 `--json` 更直接。這次沒有動模組版本號——`app.py` 的版本檢查是拿 `APP_VERSION`
去比對 `crawler` 與 `docbuilder`，只改 `app.py` 卻升版會讓比對失敗、觸發第 49 行
的 `st.error` 加 `st.stop()`，整個頁面反而起不來。純 UI 的修改不需要重載模組。

移除範圍：刪除 `history.py` 與 `history/`；`app.py` 拿掉 import、版本檢查項、蒐集
後自動存檔，以及頁面最下方整個「📚 歷史紀錄」區塊；`main.py` 拿掉 `--list-history`
／`--from-history`／`--no-history` 三個參數與對應處理；`smoke_test.py` 的模組版本
檢查由三個模組改為兩個；`.gitignore` 與 README 對應段落一併移除。三個模組版本號同
步升到 `1.5.0`，讓 Streamlit Cloud 依第 11 節的機制強制重載，避免雲端留著舊的
`history` 模組。

`Article.from_dict()` 保留——它與 `to_dict()` 對稱，`--json` 匯出與網頁版下載仍在用。

### 16. 「重新蒐集」按鈕，與它揭出來的殘留 bug

加按鈕的過程中發現一個一直存在、但因為有歷史紀錄而被遮住的問題。

勾選狀態與就地修訂是以 `pick_i`／`title_i`／`body_i` 存在 `session_state`，序號跟
著清單位置走。Streamlit 遇到已存在的 key 會**忽略 `value=`**，所以查完 A 公司、改
過第一則的標題，再查 B 公司時，B 的第一則會顯示 A 改過的標題——而且 `a.title =
new_title` 這行會把它寫回 Article 物件，產出的 Word 就真的錯了。原本清這些 key 的
程式碼只存在於歷史紀錄的「載回編輯」分支裡，第 15 節把歷史紀錄整個拿掉時，這個唯一
的清除點也跟著消失了。

所以這次做兩件事：抽出 `clear_results()`，讓「重新蒐集」按鈕與「開始蒐集」都呼叫它。
按鈕本身不直接清，而是設一個 `_do_reset` 旗標後 `st.rerun()`，在下一次執行的最開頭
才真正清除——那時 widget 還沒被建立，刪 key 不會撞上「widget 已實體化」的限制。

**測試差點寫成恆真的。** 第一版測的是「按下之後 `session_state` 還有沒有 `title_`
開頭的 key」。它會通過，但毫無意義：結果清空後那些 widget 根本不會再被建立，
`filtered_state` 自然看不到它們——把 `clear_results()` 裡的清除迴圈整段刪掉，測試
照樣全綠。真正該測的是**跨兩次蒐集**：塞進 A 的修訂、清空、再塞進 B 的結果，看 B
的標題會不會變成 A 改過的。改成這樣之後，再把清除迴圈刪掉重跑，三項失敗，其中包含
「Article 物件標題未被汙染」。

順帶補一個對照組：不清空、直接塞殘留，確認汙染真的會發生。沒有這一組，修復組通過
也不能說明什麼——它可能只是因為那個機制根本不存在。**驗證一個修復有效，前提是先
證明它要修的問題確實會發生。**

### 17. 產檔炸在一個看不見的字元上

雲端實際使用時，按下「產生 Word 檔」出現 `ValueError`，堆疊最底是 lxml 的
`_utf8`：`All strings must be XML compatible: Unicode or ASCII, no NULL bytes or
control characters`。爆點在 `doc.add_paragraph(art.title, style=S_TITLE)`。

原因是抓回來的新聞標題裡夾了一個 C0 控制字元。XML 1.0 只允許 `\t`、`\n`、`\r`
這三個控制字元，其餘 `\x00`–`\x08`、`\x0b`、`\x0c`、`\x0e`–`\x1f`，以及 `￾`／
`￿` 和落單的 surrogate 都不合法，lxml 一律拒收。這些字元多半來自新聞網站編輯
器或轉碼殘留，在網頁上看不出來，在 Streamlit 的輸入框裡也看不出來——它在整個流程
中完全隱形，直到寫進 docx 的那一刻才炸，而且是把整份檔案一起帶走。

處理方式是在 `docbuilder.py` 加一個 `xml_safe()`，在**寫入前**濾掉這些字元，並把
`\r\n`／`\r` 正規化成 `\n`。套用在所有進入文件的文字：章節標題、目錄標題、新聞
標題、來源行、副標、內文段落，以及 `make_filename()`（控制字元進檔名同樣會出事）。
反正這些字元在 Word 裡本來也顯示不了，濾掉不損失任何內容。

**為什麼清理點放在 docbuilder 而不是 crawler。** 直覺會想在抓取時就洗乾淨，但那樣
只擋得住爬蟲這一條路——使用者在網頁上就地修訂內文時貼進來的字元一樣會炸。XML 的
限制屬於「寫檔」這一層，清理就該放在這一層，才是真正的單一咽喉點。

測試一樣配對照組：先把 `xml_safe` 換成不做事的版本，確認產檔真的會失敗，再測正常
版本能產出、且產物裡沒有殘留控制字元、標題內容沒被多刪。另外驗證 `xml_safe()` 不
會誤傷正常文字（中文、換行、tab 都要留著）——清理函式最危險的失敗不是漏濾，而是
濾過頭。

三個模組版本同步升到 `1.5.1`。這次**必須**升：修的是 `docbuilder.py`，而雲端可能
還留著舊的模組在 `sys.modules` 裡，不升版就可能繼續用舊的產檔邏輯。

### 18. 推薦文章混進內文：三層擷取策略的第三層失守

實測一篇 TechOrange 的報導（HHTD 鴻海科技日），內文結尾多帶了四則不相干的新聞
標題——都是頁面下方「推薦文章」區塊的項目。

追下去發現是三層擷取策略（技術決策 4）逐層退守的結果：TechOrange 不在
`_SITE_SELECTORS` 裡，它的 JSON-LD 也沒有 `articleBody`（實測長度 0），於是退到第三層
`_densest_block()`。這一層挑「`<p>` 文字量最大的容器」，而那個容器把正文和頁尾的
Elementor 文章列表元件一起包了進去；`_paragraphs_from()` 收 `p`／`h2`／`h3`／`h4`，
推薦文章的標題正好是 `<h3 class="elementor-post__title">`，就這樣被當成小標收進來。

三道既有防線為什麼都沒擋住：

- `_END_PAT` 沒觸發。這一區的分隔文字是「更多分類文章」，包在
  `<span class="elementor-divider__text">` 裡，而 `_paragraphs_from()` 只掃
  `p`／`h2`／`h3`／`h4`，根本沒看到那個 span。
- `_DROP_PAT` 沒攔到。推薦標題本身是正常的新聞標題，不含任何雜訊字樣。
- `_trim_tail_noise()` 砍不掉。它的條件是「34 字以內且無句號」，但這幾則標題有
  40～55 字，超過門檻。

修正走結構而不是猜測，兩道一起加：

1. **`strip_noise()`**：解析前先把版面元件整塊 `decompose()` 掉，選擇器包含
   Elementor 的 `.elementor-posts-container`／`.elementor-post__title`（WordPress
   站台很常見）與 `.related-posts`／`.popular-posts` 等常見命名。
2. **`_SITE_SELECTORS` 補上 techorange.com / buzzorange.com**
   （`div.elementor-widget-theme-post-content`）。

實測兩道各自單獨啟用都能把 17 段修正為 13 段，正文與兩個 `<h2>` 小標完整保留；
兩道都拿掉才會重現 17 段。**刻意不動 `_trim_tail_noise()` 的 34 字門檻**——把門檻
調高確實也能砍掉這四則，但同時會開始誤刪真正的短段落，那是安靜的內容損毀，比多帶
幾則推薦嚴重得多。能用結構解決的事，不要用門檻去猜。

另外跑了一輪跨站回歸：Yahoo 股市、udn、財訊、自由財經、Yahoo 新聞、經濟日報六站，
加清理前後段數與字數完全一致，沒有誤刪。（財訊該篇本來就抓不到內文，屬於既有的
付費牆／動態載入問題，與這次修改無關。）

### 19. 清除輸入：兩個清除鍵的分工，與「清到哪裡為止」

第 16 節的「重新蒐集」只清結果，左側欄的輸入原封不動——那是為了「同一家公司改條件
重查」設計的。但換下一家公司時，還是得手動把公司全名、案號、統編、關鍵字一個個刪掉。
因此再加一顆「🧹 清除輸入」放在左側欄「開始蒐集」下方，兩顆分工：

| 按鈕 | 清掉什麼 | 情境 |
|---|---|---|
| 🔄 重新蒐集 | 結果與就地修訂 | 同一家公司，調條件重查 |
| 🧹 清除輸入 | 案件欄位＋結果 | 換下一家公司 |

**真正要決定的是「清到哪裡為止」。** 左側欄有兩類東西混在一起：一類是每一件都不同
的案件資料（公司、年度、案號、統編、額外關鍵字、相關企業、官網），另一類是調校參數
（近幾年、最多篇數、議題加權、保留比例、優先來源、請求間隔、章節標題、來源體例）。
調校參數是使用者調過一次就想沿用的，每換一家公司就被歸零反而礙事。所以只清第一類。

實作上，Streamlit 要重設 widget 得先有 key 才刪得掉，因此只給第一類欄位加上
`in_` 開頭的 key，`clear_inputs()` 就是刪掉所有 `in_` 開頭的 key；第二類完全不給
key，天然就不會被波及——**這個邊界不是靠一份清單維護，而是靠命名本身**，日後新增
欄位時，加不加 `in_` 前綴就決定了它屬於哪一類，不會有人忘了更新清單。

清除同樣走第 16 節的旗標＋`st.rerun()` 模式，在下一次執行的最開頭才真的刪 key。
刪掉之後 widget 回到自己的預設值，所以「年度」會回到 `115` 而不是空白——這是對的，
年度本來就幾乎不變。

測試除了驗證欄位清空，還特別驗**調校設定沒被歸零**：先把議題加權調成 300、近幾年
調成 5，清除後再確認兩者仍是 300 與 5。只測「該清的清掉了」會漏掉這個方向——清除
功能的失敗有兩種，清不掉和清過頭，後者更難察覺。

### 20. 推薦新聞第二種長相：改用連結密度判定，不靠選擇器

第 18 節用 `strip_noise()` 把版面元件整塊移除，靠的是 class 名稱。實測聯合新聞網
一篇（高科大半導體人才培育基地）又漏了四則推薦標題，但這次選擇器完全派不上用場——
那個區塊是 `section.article-content__editor` **裡面**一個**沒有 class 也沒有 id 的
裸 `<div>`**，開頭文字是「【文教熱話題】」。沒有任何 CSS 選擇器抓得到它。

而且四則擠在同一個 `<p>` 裡、用「▪」隔開，`get_text()` 一攤平就成了一段 137 字的
文字，前面三道防線又全部失效：`_END_PAT` 看不到（分隔文字在父層 div，不是
`p`／`h2`／`h3`／`h4`）、`_DROP_PAT` 攔不到（推薦標題本身是正常標題）、
`_trim_tail_noise()` 砍不掉（137 字遠超過 34 字門檻）。

改用**連結密度**判定，這是結構訊號而不是字面猜測：量測一個段落裡 `<a>` 的文字量
佔全段文字的比例。實測結果很乾淨——正文段落是 0.00～0.15，推薦清單是 1.00。

推薦清單有兩種長相，對應兩道處理：

- **多則擠成一段**（聯合新聞網）：佔比 ≥ 0.9 且連結數 ≥ 2，出現在文中任何位置
  都直接丟。
- **一則一段掛在文末**（經濟日報，實測有兩段佔比 1.00 但只有 1 個連結）：單一連結
  不能光看佔比就丟，正文引用某個標題也可能整段是連結；但**出現在文末**幾乎可以
  確定是推薦區，所以只砍文末連續的純連結段落。

門檻是量出來的不是拍的：跨八篇實測，正文最高只到 0.15，設 0.9 有很大的安全邊際。
Yahoo 股市那種「標題連結＋內文」交錯的頁面，標題段佔比 1.00 但只有 1 個連結、又
不在文末，兩道規則都不會誤傷——這是刻意保守。

測試除了驗證兩種長相都被濾掉，特別放了一個對照組：**把同樣那幾行文字的 `<a>` 標籤
拿掉、只留純文字，必須全部留著**。這一條確認判定依據是結構（有沒有連結）而不是
字面（標題長什麼樣）。把 `_LINKY` 調成不可能達到的值重跑，三項失敗、兩個對照組
照樣通過，方向是對的。

跨站回歸八篇：只有聯合新聞網那篇從 8 段變 7 段，其餘七站段數與字數完全不變。

### 21. 刪掉「自動」來源體例：把差異併進去，而不是把行為砍掉

來源標示體例原本有三個選項：自動、網址＋媒體 日期（web）、【日期/媒體】【記者】
（print）。使用者反映「自動」和 web 看起來完全一樣。

實際比對後確認：**只要新聞有網址，auto 與 web 的輸出逐字相同**；兩者只在沒有網址時
分岐——auto 會退回 print 的【】形式，web 則輸出沒有括號的裸文字。而 Google News 與
鉅亨網回來的新聞一定有網址，所以在實務資料上永遠看不出差別，等於多一個永遠不會生效
的選項。

處理方式是**把 auto 的退回行為併進 web，再把 auto 拿掉**，而不是直接刪掉 auto、讓
沒網址的新聞改用裸文字。差別在於前者不損失任何行為：`source_lines()` 的條件由
`style == "print" or (style == "auto" and not article.url)` 改成
`style == "print" or not article.url`，六種情境（有／無網址 × 有／無記者 × 無日期
× 全空）的輸出與舊版 auto 逐案一致。原本 auto 是預設值，改完之後預設 web 的行為與
原本完全相同，使用者不會感覺到任何變化。

順帶的好處是相容性自然成立：條件式現在只認 `"print"`，任何殘留的 `"auto"` 字串都會
落到 web 分支，輸出與過去相同，不需要特別處理。命令列的 `--source-style` 選項同步
改為 `web`／`print`，預設 `web`。

測試的寫法值得記一筆：**把舊版 auto 的邏輯原樣重寫一份當基準**，再逐案比對新的 web
輸出。不能拿新程式碼自己驗自己——那只會證明它跟自己一致。另外加一條「有網址時 web
與 print 必須不同」，確認剩下的兩個選項沒有變成新的重複。

### 22. 議題清單寫進說明欄：由詞庫產生，不另抄一份

「關注議題加權」的說明原本只寫「命中關稅／地緣政治／科技戰／出口管制／供應鏈轉移／
AI **等**議題」——使用者看不出完整範圍，也就無從判斷該不該勾「只保留命中關注議題的
新聞」。改成把 14 類議題全部列出，並補上供應鏈轉移的四個地區。

關鍵是**清單由 `crawler.TOPIC_KEYWORDS` 即時產生，不在 `app.py` 另抄一份**：

```python
_TOPICS = list(_crawler.TOPIC_KEYWORDS)
TOPIC_HELP = "**目前內建 %d 類議題**：%s。…" % (len(_TOPICS), "、".join(_TOPICS), …)
```

抄一份的話，日後在詞庫加了新議題卻忘了改說明，使用者看到的就是錯的，而且這種落差
不會有任何徵兆。由詞庫產生則是加了就自動出現，連數量都不用手動維護。這和第 19 節
用 `in_` 前綴劃分欄位是同一個想法：**能從既有資料推導出來的，就不要另存一份**。

同一段說明也掛到「只保留命中關注議題的新聞」那個核取方塊上——會決定要不要勾它的人，
需要的正是這份清單。順帶把「勾了之後沒命中的新聞會被剔除，篇數可能明顯變少」講明白。

測試除了確認 14 類都出現在說明裡，加了一個對照組：**在詞庫塞一個假議題、重載
`app.py`，說明必須跟著長出那一項**（測完移除）。這一條驗的不是「清單內容對不對」，
而是「說明真的是推導出來的、不是硬寫死的」——只比對字串的話，把清單複製貼上一份
也會通過。

### 23. 議題一覽表放上使用方式頁面

第 22 節把議題清單放進說明欄（tooltip），但 tooltip 要滑鼠移上去才看得到、空間也窄，
只能列出議題名稱。使用者要的是直接看得到，所以改放到落地頁的「使用方式」下方。

版面比 tooltip 寬鬆，因此改成**表格並附上主要關鍵詞**——只列議題名稱的話，看不出
「AI」是靠哪些詞觸發的（人工智慧、生成式、大模型、算力、AI 伺服器、資料中心…），
也就不知道自己關心的題材會不會被標到。每類取前六個詞、超過的加省略號，「供應鏈轉移」
單獨一列說明它的複合條件。表格同樣由 `TOPIC_KEYWORDS` 產生，維持第 22 節的原則。

三段文字各有分工：表格上方說明議題如何影響排序（加權、可調整、可設 0），表格下方
重申「關鍵詞比對不是語意判讀」的限制——這一條在已知限制第 5 點就寫過，但寫在
README 裡使用者不會看到，放在他要判斷標籤可信度的當下才有用。

一併把「重新蒐集」與「清除輸入」的用途補進使用方式，這兩顆按鈕先前只有 tooltip。

測試除了確認 14 類與關鍵詞都出現在頁面上，加了對照組：**有蒐集結果時這張表不該出現**
——它屬於落地說明，檢視結果時再佔一大塊版面只會干擾。這一條同時確認表格確實掛在
`if articles:` 的 else 分支，而不是無條件渲染。

---

## 開發歷程

| 階段 | 內容 |
|---|---|
| 1 | 讀取原始 `.doc`：以 Word COM dump 出 27 個欄位與五個自訂樣式的完整規格 |
| 2 | 驗證來源可行性：Google News RSS、Google 網址還原、鉅亨網 API 逐一實測 |
| 3 | 寫 `crawler.py`（蒐集）與 `docbuilder.py`（Word 產出） |
| 4 | 端到端測試（維格餅家、台灣禾邦電子），以 Word COM 比對產出的樣式與目錄 |
| 5 | 修正擷取雜訊：工商時報剪貼簿提示、Yahoo 文末推薦新聞 |
| 6 | 依 `新聞來源彙整.xlsx` 加入來源分級、定向檢索、來源名稱正規化 |
| 7 | 加入官網抓取與 JS 網站備援；修正時區混用問題 |
| 8 | 網頁版 `app.py`、README、本開發紀錄，推上 GitHub |
| 9 | 依回饋把來源分層改為加權排序（不限來源、只做優先），並修正同稿異版去重 |
| 10 | 修正 Big5／GB 網頁亂碼；加入關注議題詞庫與母公司／相關企業檢索 |
| 11 | 加入模組版本檢查與自動重載，修正 Streamlit Cloud 更新後的 TypeError |
| 12 | 議題改為加權並保留其他題材名額（不限於指定議題） |
| 13 | 加入歷史紀錄：自動存檔、載回編輯、重新產檔、單筆與整批下載 |
| 14 | 修正漏掉的 import；改用 AppTest 寫真正會執行主程式的冒煙測試 |
| 15 | 移除歷史紀錄功能（雲端不持久、且同 App 使用者共用，無法只靠程式解決） |
| 16 | 加入「重新蒐集」按鈕；修正跨兩次蒐集的修訂殘留，並補上測得到它的測試 |
| 17 | 修正新聞標題夾帶控制字元時的產檔失敗（lxml 拒收 XML 不合法字元）|
| 18 | 排除「推薦文章」版面元件混進內文（strip_noise），並補上 TechOrange 站台選擇器 |
| 19 | 加入「清除輸入」按鈕：以 in_ 前綴區分案件欄位與調校參數，只清前者 |
| 20 | 以連結密度濾掉推薦新聞清單（聯合新聞網那種無 class 的裸 div）|
| 21 | 刪掉「自動」來源體例：把它與 web 的差異併進 web，行為不變 |
| 22 | 關注議題清單寫進說明欄（由詞庫即時產生，不另抄一份）|
| 23 | 議題一覽表（含主要關鍵詞）放上使用方式頁面 |

---

## 驗證方式

產出的 `.doc` 用 Word COM 逐段落 dump 樣式名稱，與原始檔案比對：

- 段落樣式序列一致：`廠商 - 目錄 標 1` → `內文`（新聞目錄）→ `目錄 1`（TOC 自動產生，
  含頁碼）→ `title2.0` → `廠商 - 新聞來源` → `廠商 - 新聞內容`。
- 內文樣式規格一致：13pt、標楷體、`LineSpacingRule=4`、`LineSpacing=24`、
  `CharacterUnitFirstLineIndent=2`、`Alignment=3`（左右對齊）。
- 目錄頁碼正確生成（測試檔 15 頁，目錄頁碼 1／2／7／12／13）。

---

## 已知限制

1. **付費牆**：經濟日報、工商時報部分內容需訂閱，抓到的可能只有前段。結果會顯示
   字數，網頁版可直接補貼內文再產檔。
2. **Google 網址還原**依賴內部介面，改版可能失效；屆時全文擷取會失敗但不會中斷，
   結果會標示錯誤訊息。
3. **JS 動態官網**靜態抓不到，會退回 `site:` 定向檢索；若要完整抓取需要瀏覽器
   自動化（playwright），目前刻意不加，以免 Streamlit Cloud 部署變複雜。
4. **關鍵字命中不等於相關**：同名公司、只是順帶提及的新聞都可能入列（實測「維格餅家」
   撈到一則講香港中英街的新聞）。這是網頁版做逐則勾選確認的原因。
5. **議題標籤是關鍵詞比對，不是語意判讀**：文中順帶提到的詞也會被標上，標籤用於
   排序與人工篩選，不應直接當成分析結論。
6. **公開資訊觀測站**目前只列在來源正規化字典裡，尚未做重大訊息的直接查詢；要做需要
   公司代號而非統編。
7. 請遵守各新聞網站使用條款；程式內建請求節流（預設 0.8 秒），勿調得過短。
